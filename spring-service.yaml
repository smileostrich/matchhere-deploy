apiVersion: v1
kind: Service
metadata:                     
  name: matching-spring
  labels:
    app: matching-spring
spec:                         
  type: NodePort
  selector:
    app: matching-spring
  ports:
  - name: http
    port: 8080
    targetPort: 8080